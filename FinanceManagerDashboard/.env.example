# Base environment template. Copy one of the environment-specific files (e.g. `.env.local.example`)
# and rename it to `.env.local`, `.env.staging`, or `.env.production` before running the app.
# Values here are safe defaults that document which keys are required.

APP_ENV=local
NODE_ENV=development

API_GATEWAY_URL=http://localhost:4000
AUTH_SESSION_SECRET=local-development-session-secret-32chars!!
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/finance_manager_local?schema=public
REDIS_URL=redis://localhost:6379/0
POSTHOG_API_KEY=
PLAID_CLIENT_ID=
PLAID_SECRET=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=https://example.openai.azure.com
AWS_REGION=us-west-2
AWS_SES_REGION=us-west-2
AWS_S3_REPORTS_BUCKET=finance-manager-local-reports
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=

NEXT_PUBLIC_APP_ENV=local
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=http://localhost:8000
NEXT_PUBLIC_SENTRY_DSN=
